#!/bin/bash
# Single verification entrypoint. AGENTS.md's Definition of Done defers to this
# file, so the gate lives in exactly one place.
set -euo pipefail
cd "$(dirname "$0")/.."

PY=${PY:-.venv/bin}
"$PY/ruff" check src tests
"$PY/pytest"
